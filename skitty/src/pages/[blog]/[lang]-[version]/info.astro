---
export function getStaticPaths() {
    return [
        { params: { blog: "hello", lang: "en", version: "v1" } },
        { params: { blog: "hi", lang: "fr", version: "v2" } },
    ];
}

const { blog, lang, version } = Astro.params;
---

{blog}-{lang}-{version}
